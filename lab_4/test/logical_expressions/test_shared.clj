(ns logical-expressions.test-shared
  (:require [logical-expressions.core.operation.unary :refer :all]))

(def var-a (variable :a))
(def var-b (variable :b))
(def var-c (variable :c))
(def var-d (variable :d))
